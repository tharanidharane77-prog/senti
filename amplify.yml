version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Building static website for sentiment tracking..."
        - echo "Static files ready for deployment"
  artifacts:
    baseDirectory: /
    files:
      - 'index.html'
      - 'styles.css'
      - 'script.js'
      - '**/*'
